version: '3.8'

services:
  web:
    build: .
    container_name: icarus_0.01
    command: uvicorn rock_assistant.api:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env